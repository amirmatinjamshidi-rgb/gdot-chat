/**
 * @deprecated Use hooks/use-profile-prefs.ts (SQLCipher). Kept for type re-exports only.
 */
export type ProfilePrefs = {
  displayName: string;
  bio: string;
  phoneE164: string;
  birthday: string;
};
