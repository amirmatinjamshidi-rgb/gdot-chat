services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gdot
      POSTGRES_PASSWORD: gdot_dev
      POSTGRES_DB: gdotchat
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
